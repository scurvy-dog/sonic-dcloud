frr version 8.4-dev
frr defaults traditional
log syslog informational
log facility local4
agentx
hostname spine01
no service integrated-vtysh-config
!
password zebra
enable password zebra
!
ipv6 route fc00:0:2::/48 Loopback0
!
router bgp 65000
 bgp router-id 10.0.0.2
 bgp log-neighbor-changes
 no bgp ebgp-requires-policy
 no bgp default ipv4-unicast
 bgp bestpath as-path multipath-relax
 timers bgp 3 9
 neighbor 10.1.1.0 remote-as 65004
 neighbor 10.1.1.6 remote-as 65005
 neighbor fc00:0:ffff:: remote-as 65004
 neighbor fc00:0:ffff::6 remote-as 65005
 !
  address-family ipv4 unicast
  network 10.0.0.2/32
  neighbor 10.1.1.0 activate
  neighbor 10.1.1.0 route-map BGP-IPV6 in
  neighbor 10.1.1.6 activate
  neighbor 10.1.1.6 route-map BGP-IPV6 in
 exit-address-family
 !
 address-family ipv6 unicast
  network fc00:0:2::/48
  network fc00:0:2::1/128
  neighbor fc00:0:ffff:: activate
  neighbor fc00:0:ffff:: route-map BGP-IPV6 in
  neighbor fc00:0:ffff::6 activate
  neighbor fc00:0:ffff::6 route-map BGP-IPV6 in
 exit-address-family
exit
!
route-map BGP-IPV6 permit 20
 set ipv6 next-hop prefer-global
exit
!
ip nht resolve-via-default
!
exit
